router.post (signup) =  ('/signup')
router.post (login) = ('login)
router.put (subscriber) = ('/subscribe/:channelId')
router.put (unsubscribe) = ('/unsubscribe/:channelId')
router.put (ProfilePicUpdate) = ('/profilePic')
router.put (coverPicUpdate) = ('/coverPic')
router.post (upload) = ('/videoUpload')
router.post (like) = ('/like/:videoId')
router.post (dislike) = ('/dislike/:videoId')
router.get (videoById) = ('/videoById/:videoId')
router.delete (deleteVideo) = ('/deleteVideo/:videoId')
router.put (updateVideo) = ('/updateVideo/:videoId')
router.post (commentPost) = ('/comment/:videoId')
router.get (getAllCommentByVideoId) = ('/:videoId')
router.put (editComment) = ('/editComment/:commentId')
router.delete (deleteComment) = ('/deleteComment/:commentId')
router.put (likeComment) = ('/like/:commentId)
router.get (videoByChannelId) = ('/getAllVideo/:channelId')
router.get (channelInfo) = ('/channelInfo/:channelId')
router.get (updateChannelDetails) = ('/updateChannelDetail/:channelId')
router.delete (logout) = ('/logout/:channelId')